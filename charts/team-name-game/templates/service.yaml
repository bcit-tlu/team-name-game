apiVersion: v1
kind: Service
metadata:
  name: {{ include "team-name-game.fullname" . }}
  labels:
    {{- include "team-name-game.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "team-name-game.selectorLabels" . | nindent 4 }}
